{
  "name": "server",
  "version": "1.0.0",
  "description": "Server for news feed api",
  "main": "src/app.ts",
  "scripts": {
    "dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "production": "npm run build && node build/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.11.10",
    "nodemon": "^2.0.20",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2"
  }
}
